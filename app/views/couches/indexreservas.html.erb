<body>
	<center>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
<table class = "table center">
	<tr>
		<th> Usuario </th>
		<th> Fecha de entrada </th>
		<th> Fecha de salida </th>
		<th> Estado </th>
	</tr>


	<% @couch.reservas.all.each do |reserva| %>
		
		<tr>
			<td><p><%=user.find(reserva.user_id).nombre%>
				<%=user.find(reserva.user_id).nombre%></p>
			</td>

			<td> <%= reserva.inicio%> </td>
			<td> <%= reserva.fin%> </td>
			<td>
				<% if reserva.confirmada == true%> 
					Confirmada
				<%else%>
					Pendiente
				<%end%>
			</td>
				

	<% end %>
	<% if @couch.reservas.count == 0 %>
		<tr> <td colspan="3"> No hay reservas para este couch </td> </tr>
	<% end %>
	<tr><td class="center-block" colspan="3"><%= link_to "Atrás", :back, class: 'btn btn-info btn-sm' %></td></tr>

</table>
<div>
	<div>
	</center>
</body>