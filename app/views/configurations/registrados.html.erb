<body>
<h1>hola</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <table class = "table center">
      <tr>
        <th> <h1 style="color:#6B8E23;" class="text-center"> <strong>Lista de usuarios registrados </strong></h1> </th>
        <th> Activo </th>
        <th></th>
      </tr>
      <% @users.each do |user| %>
          <tr> 
            <td> <%= user.nombre %> </td>
            <td> <%= user.apellido %></td>
            <td> <%= user.created_at %></td>
          </tr>
      <% end %>
      <% if CouchType.count == 0 %>
        <tr> <td colspan="3"> No se encontraron coincidencias </td> </tr>
      <% end %>
      <tr><td class="center-block" colspan="3"><%= link_to "Atrás", configurations_path, class: 'btn btn-info btn-sm' %></td></tr>

    </table>
    <div>
      <div>
        </body>